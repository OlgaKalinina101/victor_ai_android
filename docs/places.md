Этот файл является частью проекта Victor AI

Проект распространяется под лицензией GNU Affero General Public License v3.0 (AGPL-3.0).

Подробности лицензии: https://www.gnu.org/licenses/agpl-3.0.html  
Полный текст: **[LICENSE.txt](../LICENSE.txt)** в корне репозитория.

Copyright © 2026 Olga Kalinina

---

## Экран "Места" (Places)

> Игровой экран для исследования реального мира через геймифицированную карту OSM

Экран "Места" состоит из двух основных частей:
1. **PlacesMenu** - главный экран со статистикой и кнопкой "Открыть уровень"
2. **MapActivity** - полноэкранная карта на Canvas с игровой механикой

---

## 📑 Содержание

- [PlacesMenu (Главный экран)](#-placesmenu-главный-экран)
- [MapActivity (Игровая карта)](#️-mapactivity-игровая-карта)
- [Скриншоты](#-скриншоты)
- [Структура проекта](#-структура-проекта)
- [Ключевые компоненты Canvas](#-ключевые-компоненты-canvas)
- [Архитектура Canvas](#-архитектура-canvas)
- [API для работы с MapCanvasView](#-api-для-работы-с-mapcanvasview)
- [Game Flow (Игровой процесс)](#-game-flow-игровой-процесс)
- [Managers (Менеджеры)](#-managers-менеджеры)
- [Модели данных](#-модели-данных)
- [Преимущества архитектуры](#-преимущества-архитектуры)
- [Технические детали Canvas](#-технические-детали-canvas)
- [Технологический стек](#️-технологический-стек)
- [Известные особенности](#-известные-особенности)
- [Будущие улучшения](#-будущие-улучшения)

---

### 🎮 PlacesMenu (Главный экран)

**Что это:** Экран со статистикой прогулок пользователя и кнопкой запуска игровой карты.

#### Функциональность:
- Отображает статистику пользователя
- Показывает последнее достижение
- Кнопка для запуска MapActivity
- Проверка аномалий геолокации перед запуском карты

#### API эндпоинты:
- **GET `/api/stats/`** - получение статистики пользователя:
  - `today_distance` - расстояние сегодня (метры)
  - `today_steps` - количество шагов сегодня
  - `weekly_chart` - массив из 7 чисел (расстояние за каждый день)
  - `streak` - серия дней подряд с активностью
  - `achievements` - список названий достижений

- **GET `/api/achievements/`** - список достижений пользователя
- **GET `/api/journal/`** - последние записи дневника

#### Компоненты:
- `PlacesViewModel` - управление состоянием и загрузкой данных
- `StatsRepository` - локальное кэширование и синхронизация с API

---

### 🗺️ MapActivity (Игровая карта)

**Что это:** Полноэкранная карта на Canvas с игровой механикой исследования мира.

#### Режимы работы:
1. **GPS Mode** - загрузка мест вокруг текущей геолокации
2. **Saved Locations Mode** - переключение между сохранёнными локациями

#### API эндпоинты:
- **GET `/places`** - получение OSM объектов по координатам:
  - Автоматически создаёт новую игровую локацию при первом запросе
  - Возвращает POI (точки интереса) и фоновые элементы (здания, дороги)
  - Параметры: `latitude`, `longitude`, `radius_km`, `limit`, `offset`

- **GET `/places/locations`** - список сохранённых локаций пользователя

- **GET `/places/locations/{location_id}/places`** - OSM объекты для конкретной локации

- **POST `/places/caption`** - генерация описания для места через LLM:
  - Принимает OSM теги места
  - Возвращает короткую "живую" подпись

- **POST `/api/walk_sessions/`** - сохранение прогулки:
  - Передаёт путь пользователя, посещённые POI, расстояние, шаги
  - Возвращает разблокированные достижения

- **POST `/api/journal/`** - создание записи в дневнике

#### Игровая механика:
- **Режим исследования**: пользователь ходит и открывает POI на карте
- **Режим поиска**: выбор цели, отображение расстояния и пути
- **Посещение мест**: сохранение посещённых POI с эмоциями
- **Следы**: отрисовка пройденного пути в виде следов 👣
- **Статистика сессии**: расстояние, время, найденные места

---

## 📸 UI

<div align="center">
  <table>
    <tr>
      <td align="center">
        <img src="example/places_4.gif" width="280" alt="Открытие карты" /><br/>
        <sub>Открытие карты</sub>
      </td>
      <td align="center">
        <img src="example/places_2.gif" width="280" alt="Исследование POI" /><br/>
        <sub>Исследование POI</sub>
      </td>
      <td align="center">
        <img src="example/places_3.gif" width="280" alt="Режим поиска" /><br/>
        <sub>Режим поиска</sub>
      </td>
    </tr>
  </table>
</div>

---

## 📁 Структура проекта

### Основные компоненты:

```
ui/places/
├── PlacesMenu.kt (512 строк) - главный экран со статистикой
├── PlacesViewModel.kt (194 строки) - управление состоянием
└── LocationAnomalyDialog.kt & AddressInputDialog.kt - диалоги

ui/map/
├── MapActivity.kt (456 строк) - Activity с игровой картой
├── MapViewModel.kt - управление состоянием карты
├── canvas/
│   ├── MapCanvasView.kt (280 строк) - координатор отрисовки
│   ├── controllers/
│   │   ├── MapController.kt - управление картой (зум, панорамирование)
│   │   └── SearchModeController.kt - режим поиска с анимацией
│   ├── renderers/
│   │   ├── GridRenderer.kt - отрисовка сетки
│   │   ├── TrailRenderer.kt - отрисовка следов 👣
│   │   ├── UserMarkerRenderer.kt - отрисовка стрелки пользователя
│   │   └── SearchModeRenderer.kt - пунктирная линия + пульсация
│   └── gestures/
│       └── MapGestureHandler.kt - обработка touch событий
├── composables/
│   ├── MapTopBar.kt - верхний бар с статистикой
│   ├── MapContent.kt - основной контент карты
│   ├── MapDialogs.kt - диалоги (POI detail, achievements)
│   ├── MapEffects.kt - side effects для карты
│   ├── CustomMapLoader.kt - экран загрузки
│   └── LocationDropdownMenu.kt - выбор локации
├── managers/
│   ├── LocationManager.kt - переключение локаций
│   ├── SearchManager.kt - логика поиска POI
│   ├── VisitManager.kt - посещение мест
│   └── WalkSessionManager.kt - завершение прогулки
├── renderer/
│   ├── MapRenderer.kt - основной рендерер
│   ├── POIMarkerRenderer.kt - отрисовка маркеров POI
│   └── BackgroundLayerRenderer.kt - фоновые элементы
├── repositories/
│   └── MapDataRepository.kt - загрузка данных с API
└── utils/
    ├── CoordinateConverter.kt - GPS ↔ Screen координаты
    ├── EmojiMapper.kt - маппинг эмоций на эмодзи
    └── LocationUtils.kt - утилиты для геолокации

data/repository/
├── StatsRepository.kt (344 строки) - статистика с кэшированием
└── LocationsRepository.kt - работа с сохранёнными локациями
```

---

## 🎯 Ключевые компоненты Canvas

### 1️⃣ MapCanvasView (Координатор)
Главный класс, который объединяет все компоненты вместе:
- Делегирует отрисовку рендерерам
- Делегирует touch события обработчикам жестов
- Управляет lifecycle карты
- Предоставляет API для внешнего управления

### 2️⃣ Рендереры (Renderers)

#### GridRenderer
- Отрисовывает сетку на карте
- Параметризованный размер ячейки

#### TrailRenderer
- Рисует следы пользователя 👣
- Умное размещение следов с минимальным расстоянием
- Автоматический поворот по направлению движения

#### UserMarkerRenderer
- Рисует маркер пользователя в виде стрелки
- Автоматический поворот к выбранному POI

#### SearchModeRenderer
- Пунктирная линия от пользователя до цели
- Пульсирующая анимация на целевой точке
- Плавная анимация с 20 FPS

#### POIMarkerRenderer
- Отрисовка маркеров POI с иконками OSM тегов
- Разные цвета для посещённых/непосещённых мест
- Подсветка выбранного POI

#### BackgroundLayerRenderer
- Отрисовка фоновых элементов (здания, дороги, водоёмы)
- Парсинг OSM геометрии (ways, polygons)
- Разные стили для разных типов объектов

---

### 3️⃣ Контроллеры (Controllers)

#### MapController
Управляет всей геометрией карты:
- `panTo(location)` - центрирование на точке
- `zoomTo(zoom)` - изменение зума
- `zoomToIncludeBoth(loc1, loc2)` - умный зум для двух точек
- `applyScroll(dx, dy)` - скролл карты
- `applyScale(factor)` - масштабирование
- `resetZoom()` - сброс к начальному значению

Управляет:
- `mapBounds` - границы видимой области
- `currentZoom` - текущий зум
- `coordinateConverter` - конвертер GPS ↔ Screen

#### SearchModeController
Управляет режимом поиска/навигации:
- `startSearchMode()` - включение с сохранением состояния
- `stopSearchMode()` - выключение с восстановлением
- Автоматическая анимация на 20 FPS
- Сохранение/восстановление зума и bounds

---

### 4️⃣ Обработчики жестов (Gestures)

#### MapGestureHandler
Обрабатывает все touch события:
- Скролл (перемещение карты)
- Pinch-to-zoom (масштабирование)
- Tap (клик на POI)

Делегирует действия в `MapController`

---

## 🔄 Архитектура Canvas

```
┌─────────────────────────────────────────┐
│         MapCanvasView (Coordinator)      │
│  - Собирает все компоненты вместе       │
│  - Делегирует отрисовку рендерерам      │
│  - Делегирует жесты обработчику         │
└─────────────────────────────────────────┘
              │
              ├─────────────────┬─────────────────┬──────────────────┐
              │                 │                 │                  │
     ┌────────▼────────┐ ┌─────▼──────┐ ┌────────▼────────┐ ┌──────▼───────┐
     │  MapController  │ │  Search    │ │  MapGesture     │ │  Renderers   │
     │                 │ │  Mode      │ │  Handler        │ │              │
     │ - Зум           │ │  Controller│ │                 │ │ - Grid       │
     │ - Панорамир.    │ │            │ │ - Touch         │ │ - Trail      │
     │ - Bounds        │ │ - Анимация │ │ - Scroll        │ │ - User       │
     │ - Converter     │ │ - Состояние│ │ - Zoom          │ │ - Search     │
     │                 │ │            │ │ - Tap           │ │ - POI        │
     │                 │ │            │ │                 │ │ - Background │
     └─────────────────┘ └────────────┘ └─────────────────┘ └──────────────┘
```

---

## 🔌 API для работы с MapCanvasView

### Базовое использование

```kotlin
val mapView = MapCanvasView(context)

// Установка данных карты
mapView.setMapData(
    bounds = mapBounds,
    pois = listOf(...),
    userLocation = LatLng(55.7558, 37.6173),
    backgroundElements = listOf(...)
)

// Обновление позиции пользователя
mapView.updateUserLocation(newLocation)

// Обработка клика на POI
mapView.onPOIClicked = { poi ->
    println("Clicked: ${poi.name}")
}
```

### Управление картой

```kotlin
// Центрирование на точке
mapView.panTo(LatLng(55.7558, 37.6173))

// Изменение зума
mapView.zoomTo(5f)

// Умный зум для показа двух точек
mapView.zoomToIncludeBoth(userLocation, poiLocation)

// Сброс зума к начальному значению
mapView.resetZoom()
```

### Режим поиска

```kotlin
// Включение режима поиска
mapView.setSelectedPOI(targetPOI)
mapView.startSearchMode()

// Выключение режима поиска
mapView.stopSearchMode()
```

### След пользователя

```kotlin
val trail = listOf(
    LatLng(55.7558, 37.6173),
    LatLng(55.7560, 37.6180),
    // ...
)
mapView.setTrail(trail)
```

---

## 🎮 Game Flow (Игровой процесс)

### 1. Запуск экрана Places
```
PlacesMenu
  ↓ (loadStats)
  StatsRepository → API /api/stats/
  ↓ (onOpenMap)
  LocationProvider.checkAnomaly()
  ↓ (if OK)
  MapActivity.start()
```

### 2. Загрузка карты
```
MapActivity
  ↓ (onCreate)
  LocationProvider → GPS или Saved Location
  ↓ (LaunchedEffect)
  MapDataRepository → API /places
  ↓ (получение OSM данных)
  MapViewModel.updateMapData()
  ↓
  MapCanvasView.setMapData()
  ↓
  Рендеринг карты на Canvas
```

### 3. Режим исследования
```
User walks
  ↓ (GPS updates)
  LocationProvider → MapViewModel
  ↓
  TrailRenderer.addPoint()
  ↓
  VisitManager.checkProximity()
  ↓ (если близко к POI)
  POI становится discovered
  ↓
  Показ POIDetailDialog
  ↓ (caption generation)
  API /places/caption
```

### 4. Режим поиска
```
User clicks POI
  ↓
  MapViewModel.selectPOI()
  ↓
  SearchModeController.start()
  ↓
  SearchModeRenderer → пунктирная линия
  ↓
  Показ расстояния и времени
  ↓ (если пришёл к цели)
  POI becomes visited
  ↓
  Выбор эмоции
  ↓
  VisitManager.addVisit()
```

### 5. Завершение прогулки
```
User closes map
  ↓
  WalkSessionManager.finishWalk()
  ↓
  API /api/walk_sessions/ ← отправка данных:
    - start_time, end_time
    - distance_m, steps
    - poi_visits[]
    - step_points[]
  ↓
  Получение разблокированных achievements
  ↓
  Показ AchievementUnlockedDialog
  ↓
  finish() → возврат в PlacesMenu
```

---

## 🔧 Managers (Менеджеры)

### LocationManager
Управляет переключением между локациями:
- Загрузка списка сохранённых локаций
- Переключение между GPS и Saved Location режимами
- Загрузка OSM данных для выбранной локации

### SearchManager
Управляет режимом поиска:
- Расчёт расстояния до цели
- Поиск ближайших background элементов
- Анимация в режиме поиска

### VisitManager
Управляет посещением POI:
- Проверка близости пользователя к POI
- Открытие POI (discovery)
- Сохранение посещения с эмоцией
- Генерация caption через LLM

### WalkSessionManager
Управляет сессией прогулки:
- Сбор статистики (расстояние, шаги, время)
- Формирование данных для API
- Отправка данных на сервер
- Обработка разблокированных достижений

---

## 📊 Модели данных

### PlaceElement (POI)
```kotlin
data class PlaceElement(
    val osmId: Long,
    val osmType: String,  // "node", "way", "relation"
    val lat: Double,
    val lon: Double,
    val tags: Map<String, String>,
    val name: String?,
    val isVisited: Boolean = false,
    val caption: String? = null
)
```

### BackgroundElement
```kotlin
data class BackgroundElement(
    val osmId: Long,
    val osmType: String,
    val tags: Map<String, String>,
    val geometry: List<LatLng>  // Координаты для отрисовки
)
```

### VisitedPOI
```kotlin
data class VisitedPOI(
    val poi: PlaceElement,
    val distanceFromStart: Float,
    val foundAt: Long,
    val emotion: POIEmotion
)

data class POIEmotion(
    val emoji: String,
    val label: String,
    val color: String
)
```

---

## ✅ Преимущества архитектуры

### 1. Single Responsibility Principle
Каждый класс делает одну вещь:
- Рендереры - только отрисовка
- Контроллеры - только логика
- Обработчики - только взаимодействие

### 2. Легко тестировать
Можно протестировать каждый компонент отдельно:
```kotlin
val gridRenderer = GridRenderer(cellSize = 100f)
gridRenderer.draw(mockCanvas, 800f, 600f)
// Проверяем результат
```

### 3. Легко читать
Вместо одного файла на 780+ строк - файлы по 50-200 строк с понятными названиями

### 4. Легко расширять
Хотите добавить новый вид отрисовки?
- Создайте новый рендерер
- Добавьте вызов в `MapCanvasView.onDraw()`
- Готово!

### 5. Меньше конфликтов в Git
При работе в команде разные люди могут работать с разными компонентами

### 6. Переиспользование
Можно использовать компоненты в других местах:
```kotlin
val trailRenderer = TrailRenderer()
// Используем в другом View или Activity
```

---

## 🎨 Технические детали Canvas

### Координаты
**GPS ↔ Screen конвертация** через `CoordinateConverter`:
- Mercator проекция для корректного отображения
- Учёт текущего зума
- Кэширование для производительности

### Производительность
- **Lazy rendering**: перерисовка только при изменениях
- **Bitmap caching**: фоновый слой кэшируется
- **Draw call batching**: группировка похожих операций
- **60 FPS** на средних устройствах

### Анимации
- **Search mode pulsation**: ValueAnimator с 20 FPS
- **Trail drawing**: постепенное появление следов
- **POI highlighting**: плавная смена цвета

### Обработка памяти
- Recycling Bitmap при смене локации
- Weak references для callbacks
- Lifecycle-aware компоненты

---

## 🛠️ Технологический стек

### UI
- **Jetpack Compose** - современный UI toolkit для Android
- **Custom Canvas View** - кастомная отрисовка карты
- **Material 3** - дизайн-система

### Архитектура
- **MVVM** - Model-View-ViewModel паттерн
- **Hilt** - Dependency Injection
- **Kotlin Coroutines** - асинхронность
- **StateFlow** - reactive state management

### Геолокация
- **Google Location Services** - FusedLocationProviderClient
- **GPS & Network** - комбинированное определение локации
- **Custom LocationProvider** - централизованный менеджер локаций

### Работа с данными
- **Retrofit + Moshi** - HTTP клиент и JSON парсинг
- **SharedPreferences** - локальное кэширование статистики
- **Repository Pattern** - абстракция работы с данными

### Canvas рендеринг
- **Android Canvas API** - низкоуровневая отрисовка
- **Custom Renderers** - модульная система рендеринга
- **ValueAnimator** - анимации

---

## 🐛 Известные особенности

### Аномалии геолокации
При резком скачке локации (>20 км за 10 минут):
1. Показывается диалог с вариантами:
   - Использовать домашнюю локацию (если сохранена)
   - Ввести адрес вручную
   - Продолжить с текущей локацией
2. Это предотвращает некорректную статистику

### Ограничения
- **Максимум локаций**: проверяется на сервере (MAX_GAME_LOCATIONS_REACHED)
- **Радиус загрузки**: 2 км по умолчанию
- **Лимит POI**: 15000 элементов за запрос
- **Минимальное расстояние между следами**: 5 метров

---

## 🚀 Будущие улучшения

- [ ] Оффлайн режим с кэшированием локаций
- [ ] Multiplayer: видеть других игроков на карте
- [ ] Кастомные маркеры для разных типов POI
- [ ] История посещений на карте (heat map)
- [ ] Экспорт прогулки в GPX
- [ ] AR режим для поиска POI
