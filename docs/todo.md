Этот файл является частью проекта Victor AI

Проект распространяется под лицензией GNU Affero General Public License v3.0 (AGPL-3.0).

Подробности лицензии: https://www.gnu.org/licenses/agpl-3.0.html  
Полный текст: **[LICENSE.txt](../LICENSE.txt)** в корне репозитория.

Copyright © 2026 Olga Kalinina

---

# Victor AI - TODO и экспериментальные фичи

> Документация по экранам в разработке и планам на будущее

---

## 🎨 Выравнивание UI

Для себя планирую подключить дизайнера, который поможет привести UI в порядок. Моей фантазии хватило только на это.

---

## 🧪 Экспериментальные экраны

Доступны только для `account_id: test_user` (тестовый режим).

### 🏠 Экран "Среда" (Environment)

**Статус:** В разработке  
**Приоритет:** Высокий (первый приоритет для будущих обновлений, дедлайн 28.02.2026)

**Описание:**  
Заглушка под будущие IoT интеграции, в первую очередь - с **Philips Hue** (умное освещение).

**Текущий функционал:**
- Определение присутствия дома через WiFi и GPS
- Статус `[дома: ✓]` или `[дома: ✗ - XXX м]`
- Настройка домашней WiFi сети (SSID + BSSID)
- Отображение текущей WiFi сети
- Пагинация списка доступных WiFi сетей (по 5 штук)

**Как работает определение "дома":**
1. Проверка подключения к домашней WiFi сети (приоритет)
2. Если WiFi не совпадает - проверка GPS (дома = расстояние ≤ 40 метров)
3. Если GPS недоступен - показывает `? м`

**Структура файлов:**
```
ui/screens/
├── EnvironmentScreen.kt            # UI экрана
└── environment/
    └── EnvironmentViewModel.kt     # Логика определения местоположения
```

**API эндпоинты:**  
Пока не используются (локальная логика на устройстве).

**Планы:**
- [ ] Интеграция с Philips Hue Bridge
- [ ] Автоматическое управление освещением для эффекта присутствия от Victor

---

### 🌐 Экран "Браузер" (Browser) - Care Bank

**Статус:** Прототип (работает на 3 позициях)  
**Приоритет:** Низкий (не первая очередь)

**Описание:**  
"Банк заботы" (Care Bank) - система автоматизации действий в WebView через экранные координаты. Позволяет Victor AI заказывать товары/услуги от имени пользователя.

**Как работает сейчас:**
1. Пользователь вводит запрос (например, "закажи нам пожалуйста блинчики с творогом, капучино, и онигири с креветкой")
2. Victor распознает этот запрос, парсит список позиций.
3. Далее:
   - Открывается WebView с заранее залогиненным аккаунтом доставки 🛴
   - Позиция вводится в поиск
   - С каждой позицией снимается скриншот
   - Скриншот отправляется на бэкенд
   - AI анализирует какая конкретная позиция из первого ряда соответствует запросу
   - Возвращается её номер (1 или 2)
   - WebView кликает по нужной позиции (через экранные координаты)
   - Добавляет в корзину и оформляет заказ

**Текущие ограничения:**
- Работает только на 3 позициях
- Привязано к конкретной доставке 🛴
- Требует заранее залогиненный аккаунт
- Использует жестко заданные экранные координаты

**Структура файлов:**
```
ui/screens/
├── BrowserScreen.kt                # Главный экран с шторкой
└── BrowserViewModel.kt             # ViewModel для банка заботы

ui/components/carebank/
├── ui/
│   ├── CareBankWebViewSheet.kt    # WebView шторка
│   └── CareBankModels.kt          # Data модели
├── actions/
│   ├── CareBankOrchestrator.kt    # Координатор действий
│   ├── CareBankHighLevelActions.kt # Высокоуровневые действия
│   └── WebViewActions.kt          # Низкоуровневые действия в WebView
├── setup/
│   ├── CareBankSetupManager.kt    # Настройка автоматизации
│   └── CareBankDataManager.kt     # Управление данными
└── helpers/
    └── CareBankScreenshotHelper.kt # Снятие и отправка скриншотов

logic/carebank/
└── CareBankCommandHandler.kt      # Обработка команд

data/repository/
└── CareBankRepository.kt          # Репозиторий для локального хранения
```

**API эндпоинты:**

- **POST `/care_bank`** - создать запись в банке заботы
  - Body: `{ emoji: "☕", value: "url search доставки", account_id: "user_id" }`
  - Response: созданная запись

- **GET `/care_bank/{account_id}`** - получить все записи пользователя
  - Response: список записей с эмодзи и значениями

- **POST `/care_bank/process-screenshot`** - обработать скриншот
  - Multipart: `account_id`, `screenshot` (файл), `query` (опционально)
  - Response: `{ position: 1 }` - номер позиции из первого ряда

- **GET `/care_bank/settings/{account_id}`** - получить настройки банка заботы
  - Response: настройки автоматизации

- **POST `/care_bank/settings`** - обновить настройки банка заботы
  - Body: `{ account_id, auto_approved, presence_address, max_order_cost, preferred_taxi_class }`
  - Response: обновленные настройки

**Настройки Care Bank:**
- **auto_approved** (Boolean) - автоматическое одобрение заказов без подтверждения
- **presence_address** (String) - адрес присутствия для доставки
- **max_order_cost** (Int) - максимальная стоимость заказа в рублях
- **preferred_taxi_class** (Enum) - предпочитаемый класс такси: `COMFORT_PLUS`, `COMFORT`, `ECONOMY`, `BUSINESS`, `MINIVAN`

**UI компоненты:**

1. **BrowserScreen** - главный экран с TODO заглушкой и кнопкой настроек (⚙️)
2. **BrowserSettingsSheet** - шторка с:
   - Заголовок "Банк заботы"
   - Поле ввода с placeholder "завтрак не вставая с кровати"
   - Кнопка "+" для сохранения записи
   - Кнопка настройки автоматизации (👆 TouchApp) - появляется после сохранения
   - Кнопка настроек банка (✏️ Edit) - открывает настройки
3. **CareBankSettingsSheet** - шторка настроек с:
   - Переключатель автоматического одобрения
   - Поле ввода адреса присутствия
   - Поле ввода макс. стоимости заказа
   - RadioButton выбор класса такси
4. **WebViewSheet** - WebView с поддержкой:
   - Загрузка URL с сохраненных записей
   - Режим настройки (setupMode) - для выбора координат кликов
   - CoordinatePicker - выбор одной координаты
   - MultiCoordinatePicker - выбор нескольких координат
   - Снятие скриншотов и отправка на анализ

**Архитектура Care Bank:**

```
Victor AI команда (из чата)
    ↓
CareBankCommandHandler
    ↓
CareBankOrchestrator
    ├── CareBankHighLevelActions (сценарии: оформить заказ, найти товар)
    │   └── WebViewActions (низкоуровневые: клик, ввод, scroll)
    ├── CareBankScreenshotHelper (снятие и анализ скриншотов)
    └── CareBankDataManager (сохранение/загрузка настроек)
```

**Data Flow:**

```
User вводит "закажи нам пожалуйста блинчики с творогом, капучино, и онигири с креветкой" → сохраняется в Room DB
    ↓
Victor получает команду "закажи нам пожалуйста блинчики с творогом, капучино, и онигири с креветкой", парсит список позиций.
    ↓
CareBankCommandHandler находит запись "☕" для доставок еды.
    ↓
Открывает WebView с сохраненным URL
    ↓
Вводит "блинчики с творогом" в поиск → скриншот
    ↓
POST /care_bank/process-screenshot → { position: 1 }
    ↓
Клик по позиции #1 (через координаты) → в корзину
    ↓
Проверка настроек (auto_approved, max_order_cost)
    ↓
Оформление заказа (если одобрено)
```

**Проблемы текущего подхода:**
- Экранные координаты ломаются при изменении размера экрана
- Привязка к конкретному WebView и аккаунту
- Нет универсальности (работает только на 3 позициях)
- Требует ручной настройки для каждой новой позиции

**Планы на будущее:**
- [ ] Универсальный конструктор макросов (UI builder)
- [ ] Распознавание элементов через Accessibility API вместо координат
- [ ] Поддержка нескольких сервисов доставки и такси
- [ ] Интеграция с AI для понимания структуры страниц
- [ ] Автоматическое логинирование через сохраненные credentials
- [ ] История заказов и отмена заказов
- [ ] Уведомления о статусе доставки

**Альтернативные подходы (рассматриваются):**
1. Интеграция напрямую через API сервисов доставки (если доступно)
2. Chrome DevTools Protocol для управления браузером
3. Puppeteer/Playwright для автоматизации (серверная сторона)
4. OCR + Computer Vision для распознавания элементов

---

## 📋 Общие TODO

### Критические фиксы перед релизом:
- [ ] Пройти [PRE_RELEASE_CRITICAL_CHECKLIST.md](../PRE_RELEASE_CRITICAL_CHECKLIST.md)
- [ ] Проверить все пункты [OPEN_SOURCE_AUDIT_REPORT.md](../OPEN_SOURCE_AUDIT_REPORT.md)
- [ ] Убрать `test_user` проверки или переключить на production режим

### Улучшения UX:
- [ ] Консистентный дизайн всех экранов (нанять дизайнера)
- [ ] Анимации переходов между экранами
- [ ] Темная/светлая тема (сейчас только темная)
- [ ] Локализация (сейчас только русский + английский в коде)

### Технический долг:
- [ ] Миграция на Compose Multiplatform (подготовка к iOS версии)
- [ ] Рефакторинг Canvas компонентов в Places
- [ ] Оптимизация Room запросов (индексы, миграции)
- [ ] Unit/Integration тесты (покрытие < 10%)

### Новые фичи:
- [ ] Push уведомления через Pushy (уже интегрировано, но не используется)
- [ ] Синхронизация между устройствами
- [ ] Экспорт/импорт данных (backup)
- [ ] Голосовой ввод в чате (через Android Speech API)
- [ ] Картинки в чате (отправка/получение)

---

## 🔗 Связанные документы

- [places.md](places.md) - Готовая документация по экрану "Места"
- [calendar.md](calendar.md) - Готовая документация по экрану "Расписание"
- [chat.md](chat.md) - Готовая документация по экрану "Чат"
- [playlist.md](playlist.md) - Готовая документация по экрану "Плейлист"
- [system.md](system.md) - Готовая документация по экрану "Системное"
- [README.md](README.md) - Обзор всей документации
