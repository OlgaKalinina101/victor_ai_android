<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ✅ ДЛЯ Pushy -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 🆕 ДЛЯ PUSHY -->
    <!-- (уже объявлено выше) -->

    <!-- 🎵 Permissions для музыкального Foreground Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- 🔔 Permissions для будильника -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- WiFi разрешения -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- 📎 Permissions для работы с изображениями -->
    <!-- Для Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Для Android 12 и ниже -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <application
        android:name=".MyApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        tools:replace="android:icon,android:label"
        android:supportsRtl="true"
        android:theme="@style/Theme.Victor_AI"
        android:enableOnBackInvokedCallback="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize|stateHidden"
            android:theme="@style/Theme.Victor_AI">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ❌ Firebase Service -->
        <!-- <service
            android:name=".MyFirebaseService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service> -->

        <!-- ✅ Pushy Job Service -->
        <service
            android:name="me.pushy.sdk.services.PushyJobService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />

        <!-- ✅ Pushy Receiver -->
        <receiver
            android:name=".MyPushReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="pushy.me" />
            </intent-filter>
        </receiver>

        <!-- 🔔 AlarmReceiver - fires exact alarms from AlarmManager -->
        <receiver
            android:name=".alarm.AlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.victor_ai.ACTION_ALARM_FIRE" />
            </intent-filter>
        </receiver>

        <!-- 🔕 Dismiss action for alarm notification -->
        <receiver
            android:name=".alarm.AlarmDismissReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.victor_ai.ACTION_ALARM_DISMISS" />
            </intent-filter>
        </receiver>

        <!-- 🔁 Restore alarms after reboot -->
        <receiver
            android:name=".alarm.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 🎵 Music Playback Service - Foreground Service для воспроизведения музыки -->
        <service
            android:name=".logic.MusicPlaybackService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Reminder Action Receiver -->
        <receiver
            android:name=".ReminderActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="REMINDER_OK" />
                <action android:name="REMINDER_DELAY" />
            </intent-filter>
        </receiver>

        <!-- 📋 Copy Text Receiver (кнопка «Скопировать» в уведомлениях) -->
        <receiver
            android:name=".CopyTextReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.victor_ai.ACTION_COPY_TEXT" />
            </intent-filter>
        </receiver>

        <!-- ❌ Firebase metadata -->
        <!-- <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/reminders_channel_id" /> -->

        <!-- ✅ Отключение Firebase -->
        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />

        <!-- Map Activity -->
        <activity
            android:name=".ui.map.MapActivity"
            android:exported="false"
            android:theme="@style/Theme.Victor_AI" />

        <!-- 🔔 Alarm Ring Activity - Full-Screen для будильника -->
        <activity
            android:name=".ui.alarm.AlarmRingActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.Victor_AI"
            android:launchMode="singleTask" />

        <!-- 🔔 Alarm Service - Foreground Service для проигрывания музыки будильника -->
        <service
            android:name=".service.AlarmService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
    </application>

</manifest>