Ага, всё ясно 🙂
У тебя **`adb` не доступен из PowerShell**, потому что его нет в `PATH`.
Но Android Studio уже содержит его внутри, просто надо правильно вызвать.

Давай покажу, как сделать, чтобы **не пересобирать ничего в сетях** и чтобы **FastAPI работал на телефоне**.

---

## **1. Где находится `adb`**

В Android Studio `adb` лежит внутри **Android SDK**.

**Как найти путь:**

* В Android Studio:
  `File` → `Settings` → `Appearance & Behavior` → `System Settings` → `Android SDK`
* Там будет путь типа:

  ```
  C:\Users\Alien\AppData\Local\Android\Sdk\platform-tools
  ```

---

## **2. Запускаем ADB напрямую**

Открываешь PowerShell **в этом каталоге**:

```bash
cd "C:\Users\Alien\AppData\Local\Android\Sdk\platform-tools"
```

Проверяешь:

```bash
.\adb devices
```

Теперь должно показать твой телефон в списке.

---

## **3. Пробрасываем порт**

Из этой же папки выполняем:

```bash
.\adb reverse tcp:8000 tcp:8000
```

---

## **4. Настраиваем Retrofit**

В `RetrofitInstance.kt` **должно быть**:

```kotlin
private const val BASE_URL = "http://127.0.0.1:8000/"
```

---

## **5. Запускаем FastAPI**

На ноутбуке:

```bash
uvicorn main:app --reload --port 8000
```

---

После этого твой телефон будет обращаться к FastAPI **по `127.0.0.1:8000`**,
и никаких проблем с сетью не будет.

---

Хочешь, я покажу, как **сделать так, чтобы Android Studio сама пробрасывала порт при запуске приложения**?
Тогда не придётся заходить в консоль каждый раз.


